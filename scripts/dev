#!/usr/bin/env bash
CONTAINER_USER=amgdev
FOLDER_NAME=workspace
NAME=$(basename "$PWD")

podman container exists "$NAME"
EXISTS=$?
RUNNING=$(podman ps --filter "name=$NAME" --filter "status=running" -q)

if [ "$EXISTS" != "0" ]; then
    echo Building container image... 
    podman build -t "$NAME" -f Containerfile $HOME/.podman

    VOLUMES = ""
    ARGS=""

    # Mount x11 socket 
    if grep -q '# USE x11' Containerfile; then
        echo "Using X11"
        VOLUMES="$VOLUMES -v /tmp/.X11-unix:/tmp/.X11-unix"
        xhost +si:localuser:amg
    fi

    # Allow KVM
    if grep -q '# USE kvm' Containerfile; then
        echo "Using KVM"
        ARGS ="$ARGS -device /dev/kvm"
    fi

    # Mount pulseaudio socket
    if grep -q '# USE pulseaudio' Containerfile; then
        echo "Using pulseaudio"
        VOLUMES="$VOLUMES -v /run/user/$UID/pulse/native:/run/user/$UID/pulse/native"
    fi

    # Check nvidia gpu presence
    if command -v nvidia-smi &> /dev/null; then
        echo "Using nvidia gpu"
        ARGS="$ARGS --gpus all"
    fi

    podman create -it \
        $VOLUMES \
        $ARGS \
        -P \
        -e DISPLAY=$DISPLAY \
        -e PULSE_SERVER=unix:/run/user/$UID/pulse/native \
        --device /dev/dri \
        --name "$NAME" "$NAME" bash

    podman start -ai "$NAME"
    exit
fi

if [ -z "$RUNNING" ]; then
    echo Starting existing container...
    podman start -ai "$NAME"
    exit
fi

echo Attaching to running container...
podman exec -it "$NAME" bash
