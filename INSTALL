INSTALL COMMANDS
- Connect to internet using nmtui
- Set up partitions and mount them
- Download and extract stage3 tarball
arch-chroot /mnt/gentoo
passwd -l root
useradd -m -G users,wheel,pipewire,plugdev,video -s /bin/bash amg
passwd amg
sbctl create-keys
sbctl enroll-keys -m
eselect repository create local
... copy dotfiles ...
... install packages ....
... copy ssh keys ...
locale-gen
rc-update add bluetooth default
rc-update add NetworkManager default
rc-update add apparmor boot
chattr +i /etc/resolv.conf
su amg -c "flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo"
flatpak override --system --no-talk-name=org.freedesktop.ScreenSaver --no-talk-name=org.freedesktop.UDisks2
